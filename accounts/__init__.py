"""Accounts package.

The account views live in ``accounts.views`` (see ``accounts/urls.py``).
Keeping this ``__init__`` empty prevents heavy imports (DRF, allauth,
simplejwt) from running while Django is populating the app registry.
"""