from __future__ import annotations


def round_mm(value: float, digits: int = 2) -> float:
    return round(float(value), digits)


def round_ratio(value: float, digits: int = 4) -> float:
    return round(float(value), digits)
